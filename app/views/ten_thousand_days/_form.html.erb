<%= form_for(ten_thousand_day) do |f| %>
  <% if ten_thousand_day.errors.any? %>
    <div id="errors">
      <h2><%= pluralize(ten_thousand_day.errors.count, "error") %> prohibited this ten_thousand_day from being saved:</h2>

      <ul>
      <% ten_thousand_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :title %><br>
    <%= f.text_field :title %><br>
    <br>
    <%= f.label :body %><br>
    <%= f.text_area :body %><br>
    <br>
    <%= f.submit %>
<% end %>